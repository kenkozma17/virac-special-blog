import Wrapper from "./Wrapper";
export default function Footer(props) {
  return (
    <footer className={`${props.className} bg-vs-green h-20 text-white`}>
      <Wrapper>test</Wrapper>
    </footer>
  );
}
